div.dashboard-open-invoice

  div(layout="row" layout-align="space-between center")
    h2 Open Invoices


    div.search
      div.form-group.addon()
        svg.icon(role="img" title="RoundedEmail")
          use(xlink:href="/images/svg/sprite.svg#icon-search")
        input.form-control(type="text" name="search" placeholder="Search Invoice" ng-model="vm.search" )

  dl.pp-grid-list(layout="column")
    dt(layout="row" layout-align="space-around center" sortable)
      div.text-left(flex="10" sort-by="Total" direction="asc") Amount
      div(flex="25" flex-offset="5" sort-by="LineItems.Description") Invoice
      div(flex="20" flex-offset="5" sort-by="Date") Issued Date
      div(flex="15" sort-by="DueDate") Due Date
      div(flex="20")

    invoice-list-item(data="invoice" type="vm.type" account-state="vm.accountState" ng-repeat="invoice in vm.invoices")
    dd(ng-show="!vm.invoices.length") There are no open invoices available